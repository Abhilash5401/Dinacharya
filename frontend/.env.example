VITE_API_URL=http://localhost:8080/api/v1

# Production (Cloudflare): keep VITE_API_URL=/api/v1 so normal API calls stay same-origin.
# Long imports skip the Worker 120s timeout by posting straight to Render:
# VITE_DIRECT_API_URL=https://dinacharya-ese5.onrender.com/api/v1
